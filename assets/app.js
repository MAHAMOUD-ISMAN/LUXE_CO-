function showToast(msg,type='success',duration=3000){const t=document.getElementById('toast');if(!t)return;t.textContent=(type==='success'?'✓ ':'✗ ')+msg;t.className='toast '+type+' show';clearTimeout(t._timer);t._timer=setTimeout(()=>t.classList.remove('show'),duration);}
function addToCart(productId,qty=1){const csrf=document.querySelector('input[name="csrf"]')?.value||'';fetch('api/cart.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:`action=add&product_id=${productId}&qty=${qty}&csrf=${encodeURIComponent(csrf)}`}).then(()=>{showToast('Produit ajouté au panier !');updateCartBadge();}).catch(()=>showToast('Erreur réseau','error'));}
function updateCart(pid,qty){const csrf=document.querySelector('input[name="csrf"]')?.value||'';fetch('api/cart.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:`action=update&product_id=${pid}&qty=${qty}&csrf=${encodeURIComponent(csrf)}`}).then(()=>location.reload());}
function removeFromCart(pid){const csrf=document.querySelector('input[name="csrf"]')?.value||'';fetch('api/cart.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:`action=remove&product_id=${pid}&csrf=${encodeURIComponent(csrf)}`}).then(()=>location.reload());}
function updateCartBadge(){const b=document.querySelector('.cart-badge');if(b){b.textContent=parseInt(b.textContent)+1||1;b.style.display='flex';}else{const ic=document.querySelector('.cart-icon');if(ic){const nb=document.createElement('div');nb.className='cart-badge';nb.textContent='1';ic.appendChild(nb);}}}
document.querySelectorAll('.product-tab').forEach(t=>t.addEventListener('click',()=>{document.querySelectorAll('.product-tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.tab-content').forEach(x=>x.style.display='none');t.classList.add('active');const el=document.getElementById('tab-'+t.dataset.tab);if(el)el.style.display='block';}));
const qd=document.getElementById('qty-dec'),qi=document.getElementById('qty-inc'),qv=document.getElementById('qty-val'),qn=document.getElementById('qty-input');
if(qd&&qi&&qv){const mx=parseInt(qv.dataset.max)||99;qd.addEventListener('click',()=>{const v=Math.max(1,parseInt(qv.textContent)-1);qv.textContent=v;if(qn)qn.value=v;});qi.addEventListener('click',()=>{const v=Math.min(mx,parseInt(qv.textContent)+1);qv.textContent=v;if(qn)qn.value=v;});}
const fBtns=document.querySelectorAll('.filter-cat'),sr=document.getElementById('search'),ss=document.getElementById('sort'),cards=document.querySelectorAll('.product-card');
let activeCat='Tout';
function applyFilters(){const q=(sr?.value||'').toLowerCase();let vis=0;cards.forEach(c=>{const cat=c.dataset.category||'',name=(c.dataset.name||'').toLowerCase();const ok=(activeCat==='Tout'||cat===activeCat)&&(!q||name.includes(q));c.style.display=ok?'':'none';if(ok)vis++;});const cnt=document.getElementById('products-count');if(cnt)cnt.textContent=vis;}
fBtns.forEach(b=>b.addEventListener('click',()=>{fBtns.forEach(x=>x.classList.remove('active'));b.classList.add('active');activeCat=b.dataset.cat||'Tout';applyFilters();}));
sr?.addEventListener('input',applyFilters);
ss?.addEventListener('change',()=>{const g=document.querySelector('.products-grid');if(!g)return;const items=[...g.querySelectorAll('.product-card')];if(ss.value==='price-asc')items.sort((a,b)=>parseFloat(a.dataset.price)-parseFloat(b.dataset.price));else if(ss.value==='price-desc')items.sort((a,b)=>parseFloat(b.dataset.price)-parseFloat(a.dataset.price));else if(ss.value==='rating')items.sort((a,b)=>parseFloat(b.dataset.rating)-parseFloat(a.dataset.rating));items.forEach(i=>g.appendChild(i));});
